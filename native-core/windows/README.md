# Parrot Native Core for Windows

The Windows native sidecar will be added later.

Shared protocol, prompts, and language catalog data live in `../shared/` and should be reused here when this sidecar is implemented.
