{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-overlay",
  "description": "Recording overlay event listener only.",
  "windows": ["recording"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
